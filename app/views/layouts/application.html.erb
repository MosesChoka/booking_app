<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Booking System" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <main>
      <%= render "layouts/components/header"%>
      <% if notice %>
        <div id="flash-notice" class="fixed top-4 left-1/2 transform -translate-x-1/2 px-6 py-4 bg-green-200 border border-green-400 text-green-800 rounded shadow-lg z-50">
          <p class="text-center font-semibold"><%= notice %></p>
        </div>
        <script>
          // Wait 3 seconds, then fade out
          setTimeout(() => {
            const notice = document.getElementById("flash-notice");
            if (notice) {
              notice.style.opacity = '0';
              // Optional: remove from DOM after fade
              setTimeout(() => notice.remove(), 500);
            }
          }, 3000); // 3 seconds
        </script>
      <% end %>
      <% if alert %>
        <p class="bg-red-200 text-red-800 p-20"><%= alert %></p>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
